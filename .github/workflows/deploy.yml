name: GitHub Pages legado

on:
  workflow_dispatch:

jobs:
  aviso:
    runs-on: ubuntu-latest

    steps:
      - name: Avisar deploy correto
        run: |
          echo "GitHub Pages foi desativado para este projeto."
          echo "O app usa rotas Next/API dinamicas; use Vercel para Web + WebView."
